@model IEnumerable<MAP_WAB2.Farmer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Famer_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Famer_Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Farmer_Mobile)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Farmer_Salary)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Farmer_Details)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Famer_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Famer_Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Farmer_Mobile)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Farmer_Salary)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Farmer_Details)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
